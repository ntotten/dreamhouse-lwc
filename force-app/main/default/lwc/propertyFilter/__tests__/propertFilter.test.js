describe('c-propertFilter', () => {
    it('filters property as expected based on type', () => {
        expect('true').toMatch('true');
    });
    it('filters property as expected based on price', () => {
        expect('true').toMatch('true');
    });
    it('filters property as expected based on distance', () => {
        expect('true').toMatch('true');
    });
});
