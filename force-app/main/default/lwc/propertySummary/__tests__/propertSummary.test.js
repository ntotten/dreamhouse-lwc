describe('c-propertySummary', () => {
    it('show the property info', () => {
        expect('true').toMatch('true');
    });
    it('shows property price', () => {
        expect('true').toMatch('true');
    });
    it('shows the property image', () => {
        expect('true').toMatch('true');
    });
});
